services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: prompt-playground
    ports:
      - "5050:5001"
    environment:
      - DOCKER_RUNNING=true
    volumes:
      - ./data:/app/data
    restart: unless-stopped
